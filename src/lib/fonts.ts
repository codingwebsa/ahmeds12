import LocalFont from "next/font/local"

export const fontHeading = LocalFont({
  src: "../../public/fonts/pilcrow.ttf",
  display: "swap",
  variable: "--font-heading",
})
export const fontBody = LocalFont({
  src: "../../public/fonts/hind.ttf",
  display: "swap",
  variable: "--font-body",
})
